{
  "name": "ksi-opencode-harness",
  "version": "0.1.0",
  "description": "Risk-aware OpenCode subagent routing and verification harness",
  "type": "module",
  "exports": "./index.mjs",
  "files": [
    "index.mjs",
    "src/",
    "agents/",
    "instructions/",
    "scripts/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test",
    "check": "node --check index.mjs && node --check src/router.mjs && node --check src/agents.mjs && node --check scripts/audit-routing.mjs && node --test",
    "audit": "node scripts/audit-routing.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "keywords": [
    "opencode",
    "agent",
    "harness",
    "routing"
  ]
}
